package com.haloword;

import java.io.File;

public class Constants {
	public static final String CONF_PATH = System.getProperty("user.dir") + File.separator  + "conf" + File.separator + "server.xml";
	public static final String WEB_ROOT = System.getProperty("user.dir") + File.separator  + "webroot";
}